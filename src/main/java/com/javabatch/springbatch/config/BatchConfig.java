package com.javabatch.springbatch.config;

/**
 * Classe para configurar o spring batch para executar no banco de dados postresql Descomentar essa
 * classe
 * 
 * @author Edmar
 *
 */
// @Configuration
public class BatchConfig {
    /*
     * @Autowired public JobBuilderFactory jobBuilderFactory;
     * 
     * @Autowired public StepBuilderFactory stepBuilderFactory;
     * 
     * @Bean public Job job() { return jobBuilderFactory.get("job").incrementer(new
     * RunIdIncrementer()).flow(step1()).end().build(); }
     * 
     * @Bean public Step step1() { return stepBuilderFactory.get("step1").<String,
     * String>chunk(1).reader(new BatchReader()).processor(new BatchProcessor()) .writer(new
     * BatchWriter()).build(); }
     */

}
